# Prompt versions

Prompt files are versioned inputs to the labeling run. Never edit a prompt file in place: create a new numbered file for every revision (for example, label_extraction_v2.md). The labeling output records both the prompt filename and its SHA-256, so each generated row remains traceable to the exact template that produced it.
